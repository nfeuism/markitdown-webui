# Installed by install.sh only on Apple Silicon Macs. Kept separate so the
# lightweight Vercel deployment does not pull MLX and model-serving packages.
mlx-vlm==0.6.6; sys_platform == "darwin" and platform_machine == "arm64"
